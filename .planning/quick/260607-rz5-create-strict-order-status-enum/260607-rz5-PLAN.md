---
quick_id: 260607-rz5
description: Create strict order status enum
date: 2026-06-07
status: completed
---

# Quick Task 260607-rz5 Plan

## Task 1: Centralize order statuses

- Add a Laravel-backed `OrderStatus` enum containing only the statuses currently used by the order lifecycle.
- Cast `Order.status` and `Order.payment_status` to the enum.
- Move status labels, badge classes, UI colors, lifecycle helpers, and calendar CSS mapping to the enum.

## Task 2: Remove random status strings

- Replace `Order::STATUS[...]` and literal status comparisons in models, factories, Livewire components, Blade views, helpers, and tests.
- Keep filters URL-compatible by accepting string query values and converting them through `OrderStatus::tryFrom`.
- Update migrations that referenced the old model constant so fresh migrations keep working.

## Task 3: Document and verify

- Update the database audit documentation and changelog.
- Add/adjust focused tests for enum casting, helper behavior, and order flows.
- Run Pint and targeted PHPUnit coverage before committing.
