---
phase: 01-platform-upgrade
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - config/mary.php
  - tests/Feature/MaryIntegrationTest.php
autonomous: true
requirements:
  - MARY-01
user_setup: []
must_haves:
  truths:
    - Mary 2 components render through the mary- prefix without taking over generic local component names.
    - Representative frontend and backend Livewire pages can render prefixed Mary components.
    - Blade view caching completes with Mary installed.
  artifacts:
    - config/mary.php declares the mary- component prefix.
    - tests/Feature/MaryIntegrationTest.php verifies prefix isolation and representative rendering.
  key_links:
    - Existing x-mary-* view tags resolve through config/mary.php.
    - Existing x-ui-* and local generic components retain their project-owned resolution.
---

<objective>
Prove Mary 2's non-colliding entry path on the upgraded runtime and asset stack.

Purpose: Satisfy the Phase 1 Mary installation contract before shared wrappers are migrated in Phase 2.
Output: A tested `mary-` prefix strategy with cached Blade compilation.
</objective>

<execution_context>
@/Users/andrejprus/Herd/birza/.codex/get-shit-done/workflows/execute-plan.md
@/Users/andrejprus/Herd/birza/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-platform-upgrade/01-CONTEXT.md
@config/mary.php
@resources/views/components/ui/button.blade.php
@resources/views/layouts/frontend/app.blade.php
@resources/views/layouts/backend/app.blade.php
@resources/views/frontend/buyer/products/index.blade.php
@resources/views/backend/countries/index.blade.php
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the prefixed Mary component contract</name>
  <files>config/mary.php, tests/Feature/MaryIntegrationTest.php</files>
  <read_first>config/mary.php, composer.json, resources/views/components/ui/button.blade.php, resources/views/components/button.blade.php</read_first>
  <behavior>
    - config('mary.prefix') equals mary-.
    - A prefixed Mary button or card renders successfully.
    - The existing local button and ui.button components still resolve independently.
  </behavior>
  <action>Create the test with `php artisan make:test --phpunit MaryIntegrationTest --no-interaction`. Render a minimal inline Blade string or test view containing both a prefixed Mary component and an existing project component. Assert the output proves distinct component resolution. Keep `mary-` as the explicit prefix per D-05; do not rename local components or make Mary unprefixed.</action>
  <verify>
    <automated>php artisan test --compact tests/Feature/MaryIntegrationTest.php</automated>
  </verify>
  <acceptance_criteria>
    - The test confirms Mary uses the mary- prefix.
    - The test confirms a prefixed Mary component and a local project component render in the same Blade tree.
  </acceptance_criteria>
  <done>The component collision strategy is executable and regression-tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove Mary rendering through frontend and backend Livewire pages</name>
  <files>tests/Feature/MaryIntegrationTest.php</files>
  <read_first>resources/views/frontend/buyer/products/index.blade.php, resources/views/backend/countries/index.blade.php, tests/Feature/Controllers/Frontend/Buyer/ProductControllerTest.php, tests/Feature/Controllers/Backend/CountryControllerTest.php</read_first>
  <behavior>
    - An authenticated buyer page containing x-mary-collapse renders successfully.
    - An authenticated admin page containing prefixed Mary controls renders successfully.
    - Neither page emits an unknown component exception.
  </behavior>
  <action>Extend the Mary integration test with representative buyer and backend requests, reusing factories and authentication helpers from sibling tests. Assert successful status, expected Livewire component, and stable Mary-rendered markup. Keep assertions structural rather than tied to vendor-internal class ordering.</action>
  <verify>
    <automated>php artisan test --compact tests/Feature/MaryIntegrationTest.php</automated>
  </verify>
  <acceptance_criteria>
    - One frontend and one backend route using x-mary-* components pass.
    - Assertions do not rely on brittle full HTML snapshots.
  </acceptance_criteria>
  <done>Mary is proven usable across both shared application shells.</done>
</task>

<task type="auto">
  <name>Task 3: Verify cached views and the integrated production bundle</name>
  <files>config/mary.php</files>
  <read_first>config/mary.php, resources/views/layouts/frontend/app.blade.php, resources/views/layouts/backend/app.blade.php</read_first>
  <action>Clear stale compiled views, cache every Blade view, and build production assets. Investigate application-owned compilation failures. Do not patch Mary vendor files for compiler warnings; record upstream-only warnings in the plan summary.</action>
  <verify>
    <automated>php artisan view:clear &amp;&amp; php artisan view:cache &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Blade view caching exits successfully.
    - The production asset build exits successfully.
    - Any remaining warning is identified as application-owned or upstream-owned.
  </acceptance_criteria>
  <done>MARY-01 is verified against both Blade compilation and the production asset pipeline.</done>
</task>

</tasks>

<verification>
- [ ] `php artisan test --compact tests/Feature/MaryIntegrationTest.php` passes.
- [ ] `php artisan view:clear && php artisan view:cache` passes.
- [ ] `npm run build` passes.
</verification>

<success_criteria>
- Mary 2 is usable through a non-colliding prefix.
- Existing local Blade components remain resolvable.
- Representative frontend and backend Mary pages render on the upgraded stack.
</success_criteria>

<output>
After completion, create `.planning/phases/01-platform-upgrade/01-03-SUMMARY.md`
</output>
