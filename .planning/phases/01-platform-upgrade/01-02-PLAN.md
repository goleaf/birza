---
phase: 01-platform-upgrade
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - config/livewire.php
  - app/Providers/AppServiceProvider.php
  - resources/views/layouts/frontend/app.blade.php
  - resources/views/layouts/backend/app.blade.php
  - tests/Unit/LivewireConfigurationTest.php
  - tests/Unit/VoltRemovalTest.php
  - tests/Feature/PlatformRoleSurfaceSmokeTest.php
autonomous: true
requirements:
  - PLAT-01
  - PLAT-02
  - PLAT-04
user_setup: []
must_haves:
  truths:
    - Livewire 4 resolves class-based components and layouts for admin, buyer, seller, and auth routes.
    - Livewire owns Alpine startup and assets are included exactly once.
    - The app contains no Volt dependency, Volt component, or Volt route.
  artifacts:
    - config/livewire.php contains the Laravel 12 and Livewire 4 runtime settings used by the app.
    - tests/Feature/PlatformRoleSurfaceSmokeTest.php covers representative routes for every role surface.
    - tests/Unit/VoltRemovalTest.php guards the standard class-component constraint.
  key_links:
    - Route::livewire declarations resolve App\Livewire classes through config/livewire.php.
    - Frontend and backend layouts include the same Livewire asset strategy.
---

<objective>
Audit and lock down the existing Livewire 4 runtime integration across all role surfaces.

Purpose: Turn the repository's partially completed upgrade into an explicit, regression-tested platform contract.
Output: Version-correct Livewire configuration and role-surface smoke coverage.
</objective>

<execution_context>
@/Users/andrejprus/Herd/birza/.codex/get-shit-done/workflows/execute-plan.md
@/Users/andrejprus/Herd/birza/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-platform-upgrade/01-CONTEXT.md
@config/livewire.php
@app/Providers/AppServiceProvider.php
@routes/admin.php
@routes/buyer.php
@routes/seller.php
@resources/views/layouts/frontend/app.blade.php
@resources/views/layouts/backend/app.blade.php
@tests/Unit/LivewireConfigurationTest.php
@tests/Unit/VoltRemovalTest.php
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify the Livewire 4 configuration and asset contract</name>
  <files>config/livewire.php, app/Providers/AppServiceProvider.php, resources/views/layouts/frontend/app.blade.php, resources/views/layouts/backend/app.blade.php, tests/Unit/LivewireConfigurationTest.php</files>
  <read_first>config/livewire.php, app/Providers/AppServiceProvider.php, resources/views/layouts/frontend/app.blade.php, resources/views/layouts/backend/app.blade.php, tests/Unit/LivewireConfigurationTest.php</read_first>
  <behavior>
    - The class namespace remains App\Livewire and generated components remain class-based.
    - Layout namespace and component_layout resolve existing Blade shells.
    - Livewire asset injection is disabled because layouts include assets explicitly.
    - Frontend and backend layouts do not double-start Alpine or duplicate Livewire assets.
  </behavior>
  <action>Extend the existing configuration test first, then make only the runtime changes needed for the installed Livewire 4 version. Remove stale provider customizations when Livewire 4 handles them natively. Keep the current standard class + Blade architecture per D-06 and preserve explicit asset ownership in both shared layouts.</action>
  <verify>
    <automated>php artisan test --compact tests/Unit/LivewireConfigurationTest.php</automated>
  </verify>
  <acceptance_criteria>
    - The Livewire configuration test covers class namespace, class make type, layout namespace, component layout, and asset injection.
    - Both shared layouts compile with one consistent Livewire asset strategy.
  </acceptance_criteria>
  <done>The Livewire runtime contract is explicit and protected by a focused unit test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add representative multi-guard route smoke coverage</name>
  <files>tests/Feature/PlatformRoleSurfaceSmokeTest.php</files>
  <read_first>routes/admin.php, routes/buyer.php, routes/seller.php, tests/Feature/Controllers/Backend/Auth/LoginControllerTest.php, tests/Feature/Controllers/Frontend/Auth/BuyerAuthControllerTest.php, tests/Feature/Controllers/Frontend/Auth/SellerAuthControllerTest.php</read_first>
  <behavior>
    - Guest admin, buyer, and seller login routes render without framework-level errors.
    - Authenticated admin, buyer, and seller dashboard routes render their expected Livewire component.
    - Each request returns a successful response or the route's intended authentication redirect, never a Livewire boot exception.
  </behavior>
  <action>Create the test with `php artisan make:test --phpunit PlatformRoleSurfaceSmokeTest --no-interaction`. Reuse model factories and existing guard conventions. Cover one public auth route and one authenticated route for each admin, buyer, and seller surface; assert Livewire component presence where the existing tests establish it. Do not duplicate detailed feature behavior already covered by controller tests.</action>
  <verify>
    <automated>php artisan test --compact tests/Feature/PlatformRoleSurfaceSmokeTest.php</automated>
  </verify>
  <acceptance_criteria>
    - The new test exercises admin, buyer, seller, and auth surfaces.
    - All tested routes render without Livewire layout, hydration, or asset exceptions.
  </acceptance_criteria>
  <done>PLAT-01 has one focused smoke suite spanning every required role surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Strengthen the no-Volt architecture guard</name>
  <files>tests/Unit/VoltRemovalTest.php</files>
  <read_first>tests/Unit/VoltRemovalTest.php, composer.json, routes/admin.php, routes/buyer.php, routes/seller.php, app/Livewire</read_first>
  <behavior>
    - Composer has no livewire/volt dependency.
    - Application PHP and Blade files contain no Volt imports, directives, or component format.
    - Route::livewire with class references remains allowed because it is a standard Livewire 4 route API.
  </behavior>
  <action>Update the existing test to scan only genuine Volt signatures and avoid treating class-based Route::livewire declarations as Volt usage. Keep the guard fast and deterministic by scanning project-owned source paths only.</action>
  <verify>
    <automated>php artisan test --compact tests/Unit/VoltRemovalTest.php</automated>
  </verify>
  <acceptance_criteria>
    - The test fails for a Volt import or dependency.
    - The test passes for the current class-based Route::livewire routes.
  </acceptance_criteria>
  <done>PLAT-04 is enforced without false positives against Livewire 4 routing.</done>
</task>

</tasks>

<verification>
- [ ] `php artisan test --compact tests/Unit/LivewireConfigurationTest.php tests/Unit/VoltRemovalTest.php tests/Feature/PlatformRoleSurfaceSmokeTest.php` passes.
- [ ] `php artisan view:cache` passes.
- [ ] `php artisan route:list --except-vendor` resolves all project routes.
</verification>

<success_criteria>
- PLAT-01, PLAT-02, and PLAT-04 are covered by focused automated checks.
- No role surface has a framework-level Livewire boot failure.
- No Volt runtime or dependency is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/01-platform-upgrade/01-02-SUMMARY.md`
</output>
