---
phase: 01-platform-upgrade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - resources/css/app.css
  - vite.config.js
  - tailwind.config.js
  - postcss.config.js
autonomous: false
requirements:
  - PLAT-03
user_setup: []
must_haves:
  truths:
    - The production asset build uses Tailwind CSS 4 and daisyUI 5.
    - Existing Laravel, Mary, WireUI compatibility, and custom application classes are discoverable by the Tailwind 4 compiler.
    - The repository no longer depends on the Tailwind 3 directive and PostCSS pipeline.
  artifacts:
    - package.json declares Tailwind CSS 4, daisyUI 5, and the Tailwind Vite plugin.
    - package-lock.json resolves one supported Tailwind CSS major.
    - resources/css/app.css uses the Tailwind 4 CSS-first entrypoint.
    - vite.config.js registers the Tailwind Vite plugin.
  key_links:
    - vite.config.js loads resources/css/app.css through Laravel Vite and @tailwindcss/vite.
    - resources/css/app.css explicitly sources application Blade, Mary, and temporary WireUI compatibility files.
---

<objective>
Move Birza's frontend build from Tailwind 3 and daisyUI 4 to the Phase 1 target.

Purpose: Complete the only confirmed platform dependency gap while preserving the brownfield UI during the later Mary migration.
Output: A locked, reproducible Tailwind 4 + daisyUI 5 Vite build.
</objective>

<execution_context>
@/Users/andrejprus/Herd/birza/.codex/get-shit-done/workflows/execute-plan.md
@/Users/andrejprus/Herd/birza/.codex/get-shit-done/templates/summary.md
@/Users/andrejprus/Herd/birza/.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-platform-upgrade/01-CONTEXT.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@package.json
@resources/css/app.css
@vite.config.js
@tailwind.config.js
@postcss.config.js
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Approve upgrade</name>
  <files>package.json, package-lock.json</files>
  <decision>Approve the Tailwind 4, daisyUI 5, and Vite dependency update.</decision>
  <context>Project rules require explicit approval before dependency changes. The current app is already on Livewire 4 and Mary 2, but package.json still pins Tailwind 3 and daisyUI 4.</context>
  <action>Pause before changing package.json or package-lock.json and present the major-version dependency change for explicit approval.</action>
  <verify>The resume signal is exactly approve or defer.</verify>
  <options>
    <option id="approve"><name>Approve upgrade</name><pros>Completes PLAT-03 and aligns the asset stack with Mary 2.</pros><cons>Requires careful visual and compatibility regression checks.</cons></option>
    <option id="defer"><name>Defer upgrade</name><pros>Keeps the current known build unchanged.</pros><cons>Leaves Phase 1 incomplete and Mary on its non-target CSS stack.</cons></option>
  </options>
  <resume-signal>Select: approve or defer</resume-signal>
  <done>The dependency decision is recorded before npm modifies the repository.</done>
</task>

<task type="auto">
  <name>Task 1: Align frontend dependencies with the supported Tailwind 4 pipeline</name>
  <files>package.json, package-lock.json, vite.config.js</files>
  <read_first>package.json, package-lock.json, vite.config.js, .planning/research/STACK.md</read_first>
  <action>After approval, use npm to install compatible stable releases in the Tailwind 4, @tailwindcss/vite 4, and daisyUI 5 major lines. Resolve Vite, laravel-vite-plugin, Flowbite, and official Tailwind plugin peer requirements from their installed metadata instead of forcing incompatible versions. Remove @tailwindcss/postcss7-compat and PostCSS-only packages only when no remaining build path consumes them. Register tailwindcss() beside the Laravel Vite plugin. Preserve D-07 through D-09 by retaining WireUI packages as temporary compatibility dependencies.</action>
  <verify>
    <automated>npm ls tailwindcss @tailwindcss/vite daisyui vite laravel-vite-plugin</automated>
  </verify>
  <acceptance_criteria>
    - package.json declares tailwindcss in the 4.x major line.
    - package.json declares daisyui in the 5.x major line.
    - package.json declares @tailwindcss/vite in the 4.x major line.
    - npm ls exits successfully without invalid peer dependencies.
  </acceptance_criteria>
  <done>The lockfile resolves a coherent Tailwind 4 build toolchain without forced or duplicate Tailwind majors.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate CSS configuration and prove the production build</name>
  <files>resources/css/app.css, tailwind.config.js, postcss.config.js</files>
  <read_first>resources/css/app.css, tailwind.config.js, postcss.config.js, resources/views/layouts/frontend/app.blade.php, resources/views/layouts/backend/app.blade.php</read_first>
  <action>Run the official Tailwind upgrade tool as a migration aid, review every generated change, and convert app.css to @import "tailwindcss". Move content discovery to explicit @source rules for resources/**/*.blade.php, Mary component PHP files, and the temporary WireUI package sources. Register daisyUI 5 and any still-required compatible plugins using their Tailwind 4-supported mechanism. Preserve the corporate and light themes, project color aliases, x-cloak rule, calendar styles, and markdown editor rules. Remove legacy @tailwind directives and obsolete config files only after all equivalent settings are represented in the new pipeline. Do not patch vendor files to silence warnings.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - resources/css/app.css contains @import "tailwindcss" and no @tailwind base/components/utilities directives.
    - npm run build exits successfully.
    - Generated CSS contains representative Mary/daisy classes and project classes used by both layouts.
  </acceptance_criteria>
  <done>The production bundle compiles on Tailwind 4 and daisyUI 5 with the existing brownfield UI sources preserved.</done>
</task>

</tasks>

<verification>
- [ ] `npm ls tailwindcss @tailwindcss/vite daisyui vite laravel-vite-plugin` passes.
- [ ] `npm run build` passes.
- [ ] `php artisan view:cache` passes.
- [ ] No vendor files were modified.
</verification>

<success_criteria>
- All tasks completed after explicit dependency approval.
- PLAT-03 is satisfied by a reproducible production build.
- No Tailwind 3 directives or compatibility package remain without a verified consumer.
</success_criteria>

<output>
After completion, create `.planning/phases/01-platform-upgrade/01-01-SUMMARY.md`
</output>
